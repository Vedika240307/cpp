#include <iostream>
#include <map>
#include <bits/stdc++.h>
using namespace std;
int main( ){
    map<int,int>map;
    for(int i=1; i<21;i++){
        if(map[i]=i*i);
    }
        for(auto i:map){
            cout<<i.first<<" , "<<i.second<<endl;
        }
    }
